pip list :
lists all pip intallations 

deactivete :
stops the environment

pip3 list
newer version of pip 

call -> activating the environment again using its path

pip install Django==2.2.4 
installs django through pip

pip freeze
lists only the installations that have been installed through pip

pip freeze > requirements.txt.

pip uninstall Django :
dleleting django

pip show Django
there is nothing to show because we deleted django in the previous command using pip


